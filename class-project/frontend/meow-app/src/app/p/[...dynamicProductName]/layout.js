export const metadata = {
  title: "Product Details - Meow App",
  description: "Detailed view of the selected product",
};

function PDPLayout({ children }) {
  return <>{children}</>;
}

export default PDPLayout;
